#!/bin/bash
set -e

CREDS_DIR="$HOME/.local/share/cage/creds"
mkdir -p "$CREDS_DIR"

exec container run -it --rm \
  --volume "$CREDS_DIR:/home/claude/.claude" \
  --env TERM \
  claude-isolated \
  claude login
